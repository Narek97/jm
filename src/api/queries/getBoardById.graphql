query GetBoardById($id: Int!) {
  getBoardById(id: $id) {
    id
    name
    description
    defaultMapId
    createdAt
    pinnedOutcomeGroupCount
    workspace {
      id
      feedbackId
      name
      journeyMapCount
      personasCount
    }
  }
}
