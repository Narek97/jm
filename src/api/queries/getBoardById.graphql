query GetBoardById($id: Int!) {
  getBoardById(id: $id) {
    id
    name
    description
    defaultMapId
    createdAt
    workspace {
      id
      feedbackId
      name
      journeyMapCount
      personasCount
    }
  }
}
